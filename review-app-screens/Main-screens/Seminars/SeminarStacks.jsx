import React from "react";
import BottomNavBar from "../../../navigation-some/BottomNavBar";


const SeminarStacks = ({ navigation, route }) => {
  return <BottomNavBar {...{ navigation, route }} />;
};

export default SeminarStacks;
